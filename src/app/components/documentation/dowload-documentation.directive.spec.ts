import { DowloadDocumentationDirective } from './dowload-documentation.directive';

describe('DowloadDocumentationDirective', () => {
  it('should create an instance', () => {
    const directive = new DowloadDocumentationDirective();
    expect(directive).toBeTruthy();
  });
});
